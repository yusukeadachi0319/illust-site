---
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';
import { formatDate } from '../../lib/format';
import { blogTitle, blogDescription } from '../../lib/site';

// 日付降順
const posts = (await getCollection('blog')).sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<Base title={blogTitle} description={blogDescription}>
  <section class="section">
    <div class="head">
      <h1 class="section-title">{blogTitle}</h1>
      <p class="lead">{blogDescription}</p>
    </div>
    <ul class="posts">
      {
        posts.map((post) => (
          <li class="post">
            <span class="date">{formatDate(post.data.date)}</span>
            <span class="title">
              <a href={`/blog/${post.id}`}>{post.data.title}</a>
              {post.data.description && <span class="desc">{post.data.description}</span>}
            </span>
          </li>
        ))
      }
    </ul>
  </section>
</Base>

<style>
  .head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 24px;
    flex-wrap: wrap;
  }
  .lead {
    margin-bottom: 24px;
  }
  .post {
    display: grid;
    grid-template-columns: 8em 1fr;
    gap: 16px;
    padding: 12px 0;
    border-top: 1px solid var(--blue);
  }
  .post:last-child {
    border-bottom: 1px solid var(--blue);
  }
  .title {
    display: flex;
    flex-direction: column;
  }
  .title a {
    color: var(--blue-dark);
  }
  @media (max-width: 560px) {
    .post {
      grid-template-columns: 1fr;
      gap: 2px;
    }
  }
</style>
